/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'
import { OptionPubkey, optionPubkeyBeet } from '../types/OptionPubkey'
import { MemberList, memberListBeet } from '../types/MemberList'

/**
 * Arguments used to create {@link LightMultisig}
 * @category Accounts
 * @category generated
 */
export type LightMultisigArgs = {
  createKey: web3.PublicKey
  configAuthority: web3.PublicKey
  threshold: number
  timeLock: number
  transactionIndex: beet.bignum
  staleTransactionIndex: beet.bignum
  rentCollector: OptionPubkey
  bump: number
  members: MemberList
}

export const lightMultisigDiscriminator = [53, 52, 200, 252, 178, 80, 144, 24]
/**
 * Holds the data for the {@link LightMultisig} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class LightMultisig implements LightMultisigArgs {
  private constructor(
    readonly createKey: web3.PublicKey,
    readonly configAuthority: web3.PublicKey,
    readonly threshold: number,
    readonly timeLock: number,
    readonly transactionIndex: beet.bignum,
    readonly staleTransactionIndex: beet.bignum,
    readonly rentCollector: OptionPubkey,
    readonly bump: number,
    readonly members: MemberList
  ) {}

  /**
   * Creates a {@link LightMultisig} instance from the provided args.
   */
  static fromArgs(args: LightMultisigArgs) {
    return new LightMultisig(
      args.createKey,
      args.configAuthority,
      args.threshold,
      args.timeLock,
      args.transactionIndex,
      args.staleTransactionIndex,
      args.rentCollector,
      args.bump,
      args.members
    )
  }

  /**
   * Deserializes the {@link LightMultisig} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [LightMultisig, number] {
    return LightMultisig.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link LightMultisig} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<LightMultisig> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(`Unable to find LightMultisig account at ${address}`)
    }
    return LightMultisig.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'SQDS4ep65T869zMMBKyuUq6aD6EgTu8psMjkvj52pCf'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, lightMultisigBeet)
  }

  /**
   * Deserializes the {@link LightMultisig} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [LightMultisig, number] {
    return lightMultisigBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link LightMultisig} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return lightMultisigBeet.serialize({
      accountDiscriminator: lightMultisigDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link LightMultisig} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args: LightMultisigArgs) {
    const instance = LightMultisig.fromArgs(args)
    return lightMultisigBeet.toFixedFromValue({
      accountDiscriminator: lightMultisigDiscriminator,
      ...instance,
    }).byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link LightMultisig} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    args: LightMultisigArgs,
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      LightMultisig.byteSize(args),
      commitment
    )
  }

  /**
   * Returns a readable version of {@link LightMultisig} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      createKey: this.createKey.toBase58(),
      configAuthority: this.configAuthority.toBase58(),
      threshold: this.threshold,
      timeLock: this.timeLock,
      transactionIndex: (() => {
        const x = <{ toNumber: () => number }>this.transactionIndex
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      staleTransactionIndex: (() => {
        const x = <{ toNumber: () => number }>this.staleTransactionIndex
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      rentCollector: this.rentCollector,
      bump: this.bump,
      members: this.members,
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const lightMultisigBeet = new beet.FixableBeetStruct<
  LightMultisig,
  LightMultisigArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['createKey', beetSolana.publicKey],
    ['configAuthority', beetSolana.publicKey],
    ['threshold', beet.u16],
    ['timeLock', beet.u32],
    ['transactionIndex', beet.u64],
    ['staleTransactionIndex', beet.u64],
    ['rentCollector', optionPubkeyBeet],
    ['bump', beet.u8],
    ['members', memberListBeet],
  ],
  LightMultisig.fromArgs,
  'LightMultisig'
)
